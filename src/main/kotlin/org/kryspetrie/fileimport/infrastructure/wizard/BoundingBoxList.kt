package org.kryspetrie.fileimport.infrastructure.wizard

/**
 * Re-export of BoundingBoxList from domain layer. Maintains backward compatibility while the
 * canonical definition lives in [org.kryspetrie.fileimport.domain.model.geometry].
 */
typealias BoundingBoxList = org.kryspetrie.fileimport.domain.model.geometry.BoundingBoxList
